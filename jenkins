pipeline {
    agent any
    stages {
        stage("Docker Version") {
            steps {
                sh "docker --version"
            }
        }
        stage("gi Version") {
            steps {
                sh "git --version"
            }
        }
    }
}
