pipeline {
    agent any
    stages {
        stage("Docker Version") {
            steps {
                sh "docker --version"
            }
        }
        stage("github Version") {
            steps {
                sh "git --version"
            }
        }
    }
}
